-- 017：视觉策划方案（2026-09-02 反模板化：向 ChatGPT 网页工作模式靠拢）
-- art_director 节点在 page_split 之后、asset_gen 之前，用一次 LLM 为 6 页各出
-- 一份创意 brief（构图/文字载体/色调/视觉元素/信息焦点 + 示意图用结构化版面），
-- 替代固定的 6 构图×6 文字形式轮换（创意词汇表池子小、跨任务撞款是模板感根源）。
-- 底座铁律（字体规范/伪汉字禁令等）仍由代码保证，方案只管创意层。
-- 任务停在 confirm_gen（待生图）时前端展示方案卡片供人工确认/编辑/重策划；
-- 方案随任务冻结，定点重生成沿用同一方案不漂移。NULL=未策划/策划失败（回退轮换）。
ALTER TABLE tasks ADD COLUMN IF NOT EXISTS plan_json JSONB;
