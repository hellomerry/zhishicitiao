# 产能验证平台 — 程序使用说明

- 版本：S0（阶段 0 开发完成）
- 打包日期：2026-08-14
- 用途：3 人协同验证「月产 10 万条图文内容」所需的人员、算力、成本

---

## 一、项目概述

这是一个 **AI 图文内容产能验证平台**。系统自动完成「生成正文 + 证据核查 + 搜实景图 + 生成 6 张配图」，3 名审核员（文案事实 / 图片版权 / 合规交付）在 Web 工作台审核，平台采集真实产能数据（Q3），用于推算月产 10 万条的资源配置。

### 接入的大模型（API key 已配置在 `.env`）

| 模型/服务 | 用途 | 供应商 |
|---|---|---|
| DeepSeek V4-Pro | 正文生成 + 生图提示词 + 联网交叉验证 | DeepSeek |
| Kimi K3（Kimi Code）| 校稿检查 | Moonshot |
| z-image-turbo | 生图（3:4 竖版，中文渲染优）| 阿里云百炼 |
| 豆包搜索 | 证据包检索（结构化来源）| 豆包/Feedcoop |
| OpenSERP | 搜实景图（免费自托管）| 开源 |

---

## 二、环境要求

| 组件 | 版本 | 说明 |
|---|---|---|
| Python | 3.12+ | 建议 3.12/3.13 |
| uv | 任意 | Python 包管理器 |
| PostgreSQL | 16+ | 数据库 |
| Redis | 7+ | 缓存/队列 |
| Docker / OrbStack | 任意 | 跑 OpenSERP（搜图服务）|

> macOS 用 Homebrew 安装：`brew install postgresql@16 redis uv`，Docker 用 OrbStack 或 Docker Desktop。

---

## 三、安装部署（首次使用）

### 0. 一键启动（推荐）

首次部署完成后，日常启动只需一条命令：

```bash
cd query-validation-platform
uv sync --extra dev    # 首次才需要
bash start.sh          # 自动检查 PG/Redis/OpenSERP → 建库建表 → 启动
```

`start.sh` 会自动完成：启动 PG/Redis → 建数据库 → 建 21 张表 → 初始化三个账号 → 启动 OpenSERP → 启动 Web 服务。幂等设计，重复执行安全。

### 1. 进入程序目录

```bash
cd query-validation-platform
```

### 2. 安装 Python 依赖

```bash
uv sync --extra dev
```

### 3. 启动 PostgreSQL 和 Redis

```bash
# 方式一：Homebrew
brew services start postgresql@16
brew services start redis

# 方式二：Docker
docker compose up -d
```

> 注意：`brew services start postgresql@16` 在部分 Homebrew 版本有 bug，可改用：
> `/opt/homebrew/opt/postgresql@16/bin/pg_ctl -D /opt/homebrew/var/postgresql@16 -l /tmp/pg16.log start`

### 4. 创建数据库和用户（首次）

```bash
PGBIN="/opt/homebrew/opt/postgresql@16/bin"
"$PGBIN/psql" -d postgres -c "CREATE USER qvp WITH PASSWORD 'qvp';"
"$PGBIN/psql" -d postgres -c "CREATE DATABASE qvp OWNER qvp;"
```

### 5. 初始化数据库表（21 张表）

```bash
PGPASSWORD=qvp /opt/homebrew/opt/postgresql@16/bin/psql -h localhost -U qvp -d qvp -f migrations/001_initial_schema.sql
```

### 6. 配置 API key（`.env` 已预填）

`.env` 文件已包含所有模型的真实 key，无需修改。字段说明：

```
DEEPSEEK_API_KEY=sk-...     # DeepSeek（正文生成+验证）
KIMI_API_KEY=sk-kimi-...    # Kimi Code（校稿）
DASHSCOPE_API_KEY=sk-...    # 阿里百炼（z-image-turbo 生图）
DASHSCOPE_BASE_URL=...      # 百炼 workspace 地址
DOUBAO_SEARCH_KEY=...       # 豆包搜索（证据包）
DOUBAO_ARK_KEY=             # 豆包方舟（预留，未用）
```

### 7. 启动 OpenSERP（搜图服务，可选但推荐）

```bash
docker run -d --name openserp -p 7001:7000 karust/openserp serve -a 0.0.0.0 -p 7000
```

> 注意端口 7001（7000 被 macOS AirTunes 占用）。

### 8. 启动 Web 服务

```bash
uv run uvicorn src.api.main:app --host 0.0.0.0 --port 8000
```

### 9. 初始化三个用户（首次）

```bash
uv run python -c "
import asyncio, hashlib
from sqlalchemy import text
from src.db.session import SessionLocal

async def main():
    async with SessionLocal() as s:
        pw = hashlib.sha256('12345678'.encode()).hexdigest()
        for n, r in [('张三','A'),('李四','B'),('王五','C')]:
            await s.execute(text('INSERT INTO users (name, role, password_hash) VALUES (:n,:r,:p) ON CONFLICT (id) DO NOTHING'), {'n':n,'r':r,'p':pw})
        await s.commit()
asyncio.run(main())
"
```

---

## 四、使用流程

### 访问地址

浏览器打开 `http://<本机IP>:8000/`（本机 IP 用 `ipconfig getifaddr en0` 查看）。

### 登录账号

| 用户名 | 角色 | 密码 |
|---|---|---|
| 张三 | A · 文案事实审核 | 12345678 |
| 李四 | B · 图片版权审核 | 12345678 |
| 王五 | C · 合规交付审核 | 12345678 |

### 工作流程

1. **导入 Query**（`/import`）：粘贴查询词（每行一个），选内容类型，点「导入并启动」
2. **系统自动生成**：后台跑 13 节点流水线（正文+证据+搜图+生图）
3. **进度查看**（`/progress`）：看任务状态和节点完成数
4. **审核**（`/workbench`）：3 个审核员各自登录，领取 → 审核 → 通过/驳回
5. **看板**（`/dashboard`）：看产能、成本、绿黄红分布

---

## 五、目录结构

```
query-validation-platform/
├── src/                    # 后端代码
│   ├── api/                # FastAPI 接口（auth/tasks/review/dashboard）
│   ├── pipeline/           # 13 节点流水线
│   ├── gateway/            # 模型网关（DeepSeek/Kimi/豆包/z-image/OpenSERP）
│   ├── models/             # SQLAlchemy 数据模型
│   ├── review/             # 审核工作台/心跳/异常/批次
│   ├── quality/            # 规则引擎/图文一致性
│   ├── risk/               # 风险分类
│   └── dashboard/          # 看板指标/精度报告
├── static/                 # 前端（登录/工作台/导入/进度/看板）
├── tests/                  # 34 个测试
├── migrations/             # 建表 SQL
├── .env                    # API key（真实）
├── pyproject.toml          # 依赖配置
└── docker-compose.yml      # PG+Redis
```

---

## 六、Claude Code 会话缓存

本打包包含 `claude-session-cache/` 目录，是开发过程中 Claude Code 的完整会话记录（JSONL 格式）。

- 文件：`claude-session-cache/*.jsonl`
- 用途：记录整个开发过程（需求分析 → 设计 → 评审 → 实现 → 模型接入），可用于恢复开发上下文
- 原位置：`~/.claude/projects/-Users-zhangyanlei-Desktop-Query-------V2-PPT----2026-8-11-----/`
- 恢复方式：把 jsonl 放回该目录，Claude Code 即可读取历史会话

---

## 七、常见问题

| 问题 | 解决 |
|---|---|
| 端口 8000 被占 | `lsof -i :8000` 找进程 kill，或换端口 `--port 8001` |
| 端口 7000 被 AirTunes 占 | OpenSERP 用 `-p 7001:7000` |
| 生图报 DataInspectionFailed | 内容触发阿里审核，换 Query 或改生图 prompt 前缀 |
| 生图报 429 限流 | node_asset_gen 已用 Semaphore(2) 限并发，若仍触发降低并发 |
| 数据库连不上 | 确认 PG 已启动，`pg_isready` 检查 |
| 模型调用失败 | 检查 `.env` 的 key 是否有效、额度是否用尽 |

---

## 八、测试

```bash
cd query-validation-platform
uv run python -m pytest tests/    # 34 个测试
```

---

## 九、多人开发协作（同事一起测试/开发）

### 方式一：各自部署（推荐，方便改代码）

每个同事在自己的 Mac 上解压本包，按「一键启动」部署一个完整实例：

1. 解压 `产能验证平台-S0打包.tar.gz`
2. `cd query-validation-platform && uv sync --extra dev && bash start.sh`
3. 浏览器访问 `http://<同事IP>:8000` 登录

**注意**：
- API key 是共享的（都在 `.env` 里），大家用同一批 key，注意额度
- 各自的数据库是独立的（各建各的 qvp 库）
- 生图/搜图服务各自本地跑（OpenSERP 各自启动）

### 方式二：共享一个服务（只想测试审核，不改代码）

不用部署，直接浏览器访问部署好的那台机器：

```
http://<部署机IP>:8000
```

三个账号轮换登录即可体验三角色审核。

### 协同开发建议

- **代码同步**：用 git（仓库 `zhangleifire-coder/query-validation-platform`）或直接互传修改的文件
- **数据库**：各自独立，避免数据混淆；正式验证时统一用一台机器
- **API key 保密**：`.env` 含真实 key，不要提交到公开仓库、不要发到群

### 改代码后重启

```bash
# 改了 Python 后端，需要重启（Ctrl+C 停掉再启动）
bash start.sh

# 只改了前端（static/*.html），刷新浏览器即可，无需重启
```
