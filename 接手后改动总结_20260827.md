# 接手后改动总结（2026-08-23 ～ 2026-08-27）

> 基线：`401e944`（2026-08-23 移交入库）。此后共 **23 个提交**，全部推送到
> GitHub（`hellomerry/zhishicitiao`，`feat/three-modes` + `main` 双分支），
> 并逐批部署到生产服务器 **vpstest04**（`~/qvp`，docker compose，app 容器
> `qvp-app-1` 直挂 src/static 目录）。数据库迁移由 entrypoint 自动应用，
> 本期新增 3 个迁移（007/008/009）均已在生产应用。

## 一、出图质量攻坚战（本期主线）

针对「图中文字异体变形/扭曲」这一最高频客诉，分三步治理，最终上了终极方案：

1. **出图文字质检门**（`bc777b0`）：每张配图生成后用 qwen-vl-ocr 识别图上文字，
   与分页文案做字级相似度对撞，低于 0.85 自动换构图重生成（最多 2 次），
   仍不达标打 `text_garble` 标记交人工。同时分页文案减量。
2. **文字渲染加固**（`89e5e03`）：诊断出"异体变形"根因是模型混入日文新字体
   （実/対/変等）且 OCR 会脑补纠错形成质检盲区。提示词点名「中国大陆规范简体
   字形、严禁日文新字体/繁体/异体/自造字」「文字视为逐字精确复制的排版内容」；
   生图请求加 `quality=high`（网关不认识该参数时自动降级去掉重试，防 openox
   兼容问题）。
3. **文字后期合成·终极方案**（`c6f191f`）：架构性解决——**AI 只画无字背景**
   （新增全套无字版提示词，严禁画面任何文字并按页指定留白区），分页文案由
   `text_composite.py` 用 Noto Sans SC 真实字体程序化合成：半透明面板深浅随
   背景亮度自适应、标题/正文智能拆分、CJK 逐字换行、自动缩字号、输出固定
   1152x1536。图上每个字都是 Unicode 真实字形，**变形/伪汉字从根上不可能再
   出现**。`TEXT_COMPOSITE_ENABLED=false` 可整体回退；字体缺失自动降级。

配套：**生图提示词对齐用户生产环境真实提示词**（`94489dc`，里程碑 tag
`prompt-align-20260826`）——信息增益、风格锚定封面、各页文字不重复，
字数上限 40→100。

## 二、配图版本体系（路线 A：定点重生成）

- **版本保留+一键换回**（`6e2a786`，迁移 009 `assets.is_active`）：定点重生成
  的旧图不再删除，降级为历史版本保留；可对比后「设为正式」换回，交叉校验与
  风险分级随正式版重建。
- **版本标注+驳回自动提交修正**（`9c470ab`）：图注区分「初版/修正版 第N版/
  历史 第N版」；审核驳回后**自动**提交重生成，不再人工回任务中心点重试。
- **历史版本标注驳回原因**（`c886f1a`）：按标记时间与版本生成时间区间归因，
  历史图注下显示「驳回：配图-图上有乱码」式原因。
- **全屏图片查看器**（`00e94b4`）：Lightbox 替代小预览窗，支持翻页、缩放、
  图文对照。

## 三、协作流与权限

- **创建者自助修正**（`03b9361`）：任务中心详情新增「✎ 标记修正」——创建者
  对自己的任务做定点标记（文案/配图 + 问题说明）即自动重生成标记项，
  不必等审核员驳回；严格属主校验（`POST /api/tasks/{id}/fix`）。分工变为
  **创建者自查自修、审核员只做终审**。
- **任务归属隔离**（`d149f97`，迁移 008）：非 admin 仅见/操作自己创建的任务
  （列表/详情/回收站/导出），历史任务回填 admin 所有。
- **admin 可审核全部任务**（`c277ca3`）：队列按任务聚合并显示 open_roles，
  admin 任选一个开放角色会话领取审核。
- **purge 权限修复**（`2c11586`）：彻底删除改服务端校验 admin 密码，修掉
  非 admin 可彻底删除的漏洞。

## 四、回收站体系

- **回收站**（`e1de474`，迁移 007）：任务软删除/恢复/admin 彻底删除。
- **导出后自动入站**（`7c13797`）：导出已通过内容包的任务自动移入回收站。
- **批量操作**（`3954d55`）：任务中心勾选批量移入、回收站批量恢复。

## 五、参考图治理（single/compare 模式）

- **质量过滤+高清搜索词+人工换图**（`e793bf6`）：候选图逐张下载 PIL 校验
  分辨率（≥640x480），择优本地化；支持人工删除低质图、自定义词重搜。
- **对比类标注搜索标的**（`88846e4` + `0f1dbfe`）：compare 模式拆 A:/B:
  主体分搜实景图，生图按主体分池喂参考图（每页 A、B 各 2 张轮换），
  手动补图可标注归属主体。

## 六、易用性与前端

- **全站列表排序**（`c505fa2`）：任务中心/回收站服务端 sort/order +
  各板块表头点击/下拉排序。
- **风险原因中文化**（`514b1e2`）：风险判定与原因前端中文映射，不再显示英文。
- **index.html no-cache**（`ce8f9cf`）：SPA 入口禁缓存，彻底解决部署后
  用户拿到旧版前端的问题。
- **审核台视野优化**（`baacf16`）：页面加宽至 1760px、待审队列吸附独立
  滚动、队列可折叠让主区全宽、队列项紧凑化。

## 七、基础设施修复

- **迁移 SQL 显式 UTF-8**（`0bb8727`）：修 Windows GBK locale 下 pytest/
  init_db 崩溃。

## 验证与测试

- 每批改动均：pytest 相关套件全绿 → 提交推送 → scp 部署 → md5 逐文件校验 →
  docker compose restart → healthz 验证。本期新增测试约 40 例（文字质检门、
  归属隔离、回收站、参考图过滤、版本换回、驳回归因、quality 降级、
  文字合成器 20 例、自助修正 7 例等）。
- 部署均避开生产队列繁忙时段（查 `queue_size`/`in_flight` 空闲才重启，
  曾挂后台看守任务等队列空闲后自动重启）。

## 当前生产配置要点

- 生图：`gpt-image-2` / 1152x1536 / quality=high，经 openox 转发；
  文字合成开启（无字背景 + Noto Sans SC 程序化合成）。
- 已验证（三层证据）：容器进程内 `IMAGE_MODEL=gpt-image-2`、
  今日落库配图 `model_version` 全部为 `gpt-image-2`。
