# 开发交接指南（产能验证平台）

- 日期：2026-08-18
- 分支：`feat/three-modes`
- 用途：交接给 KimiCode 重新开发 Web 前端 + 调整程序

---

## 1. 项目是什么

「产能验证平台」——从 Query（一个用户搜索词）自动生产一条图文内容（1 篇正文 400-700 字 + 6 张配图），并走完审核/风险分流/批次会签的验证流水线。核心目的是**测量产能**（3 人/6 人稳定合格产能 Q3/Q6），不是内容平台本身。

**一条内容 = 1 篇正文 + 6 张图（1 封面 + 5 内页）**，不含发布。

---

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Python 3.12 + FastAPI + uvicorn |
| ORM | SQLAlchemy 2.0（async）+ asyncpg |
| 数据库 | PostgreSQL 16（本地 `qvp` 库，测试用 `qvp_test`） |
| 缓存/队列 | Redis（进程内事件总线，暂未重度用 Redis） |
| 文本模型 | DeepSeek（主）+ Kimi（兜底），走 litellm |
| 生图模型 | gpt-image-2（经 openox.net 代理，可模拟） |
| 搜图/搜索 | OpenSERP（实景图）、豆包搜索 API（证据包） |
| 前端 | Vue 3 + Vue Router SPA（`static/`，本地 vendor 无构建链，hash 路由） |
| 测试 | pytest（asyncio_mode=auto），49 个用例 |

**依赖版本**见 `query-validation-platform/requirements.txt` / `pyproject.toml`。

---

## 3. 目录结构（关键部分）

```
query-validation-platform/
├── .env                  # 环境变量（API key 等，gitignored）
├── pyproject.toml        # 依赖 + pytest 配置
├── migrations/001_initial_schema.sql   # 21 张表（幂等）
├── src/
│   ├── api/              # FastAPI 路由
│   │   ├── main.py       # app 入口 + 静态页挂载
│   │   ├── auth.py       # 登录/注册（张三/李四/王五）
│   │   ├── tasks.py      # 导入 Query、任务统计、随机抽查
│   │   ├── stream.py     # SSE 流式事件 + 快照
│   │   ├── admin.py      # 后台管理（周期/导出/清空/日志）
│   │   └── review.py / dashboard.py / entities.py / healthcheck.py
│   ├── pipeline/         # 14 节点流水线
│   │   ├── nodes.py      # 各节点实现 + execute_node 埋点
│   │   ├── orchestrator.py  # run_pipeline 串行执行
│   │   └── idempotency.py   # 节点幂等（node_idempotency_key）
│   ├── gateway/          # 外部 API 适配
│   │   ├── image_gen.py  # 生图（gpt-image-2，可 mock）
│   │   ├── prompt_versions.py  # 6 条模式提示词
│   │   ├── failover.py   # 文本模型主备切换
│   │   ├── litellm_adapter.py  # litellm 封装
│   │   ├── image_search.py / web_search.py
│   ├── stream/           # 队列/调度/事件总线/进度
│   │   ├── scheduler.py  # 队列 + worker 池 + 自适应并发
│   │   ├── bus.py        # 进程内 pub/sub
│   │   ├── progress.py   # 每任务 debug 日志 + 进度快照
│   │   ├── limiter.py    # AIMD 自适应并发
│   │   └── maintenance.py  # 23h/1h 工作周期
│   ├── models/           # SQLAlchemy 模型（tasks/assets/drafts/...）
│   ├── config.py         # 所有配置（pydantic-settings）
│   └── quality/ risk/ review/ dashboard/  # 业务逻辑
├── static/               # Vue3 SPA 前端（index.html/app.js/api.js + views/ + components/ + vendor/）
│   └── fonts/            # 思源宋体 Noto Serif SC Webfont（已接入）
└── tests/                # 49 个测试
```

---

## 4. 核心架构

### 4.1 14 节点流水线（`src/pipeline/nodes.py`，2026-09-01 起 13→14）

```
task_import → entity_bind → evidence_build → draft_gen → draft_polish → rule_check
→ page_split → asset_gen → ocr_read → cross_check → risk_classify
→ review_queue → batch_signoff → publish_snapshot
```

- `execute_node` 是统一封装：做幂等检查、记录耗时、发布 `node_started/finished/failed` 事件（失败带完整 traceback）。
- `run_pipeline(task_id)` 串行跑这 14 个节点。
- **`draft_polish` 校稿润色（2026-09-01 新增，对齐人工生产流程 P4）**：draft_gen 后、rule_check 前——删夸大表述/削 AI 腔/小标题 ≤8 字/全文 ≤700 字/补免责声明；LLM 失败自动沿用原稿不阻塞。`GET /api/meta/nodes` 是前端节点名唯一数据源，已同步 14 节点。
- **`page_split`（2026-08-20 重写）**：不再机械切割正文，而是用 LLM 把整篇正文改写成 6 页图上文案（`PAGES_PROMPT`：封面/要点×4/结尾，纯文本供图上排版），调用失败自动退回 `_split_pages` 机械保序分段。`asset_gen` 的生图提示词直接引用分页文案，文案质量决定图质量。
- **生图前人工确认门（2026-08-27，`config.image_gen_confirm_gate` 默认开）**：调度器对 `kind="pipeline"` 的任务以 `run_pipeline(stop_after="page_split")` 只跑前 6 个节点（找图+生文+分页），任务停在 **`confirm_gen`（待生图）**；创建者（或 admin）在任务详情检查正文/分页文案/参考图（可重搜/上传/删参考图）后点「▶ 确认并开始生图」→ `POST /api/tasks/{id}/start_gen`（严格属主校验，仅 confirm_gen 可放行）→ 以 `kind="gen_resume"` 入队续跑全链（`page_split` 及之前节点幂等跳过；若确认期间改动了参考图，幂等 key 变化会自动重跑对应节点），完成进 `review`。生图是最贵步骤，确认门避免为不合格文案/参考图浪费算力。`confirm_gen` 可移入回收站。关门（`IMAGE_GEN_CONFIRM_GATE=false`）则恢复旧行为一把跑完。

### 4.2 三种生产模式（`tasks.mode`）

| mode | 含义 | 正文提示词 | 生图方式 | 生图提示词 |
|---|---|---|---|---|
| `general` 通用 | 通用科普/教程 | draft_general_v1 | 纯文生图 | image_general_v1 |
| `single` 单品 | 单一产品深测 | draft_single_v1 | 图生图（搜实景图做参考） | image_single_v1 |
| `compare` 对比 | 两主体对比 | draft_compare_v1 | 图生图（参考图） | image_compare_v1 |

- 模式在**导入时手动指定**（`import.html` 下拉 + `import_queries`/CSV 的 `mode` 字段）。
- 提示词在 `gateway/prompt_versions.py`，`get_draft_prompt(mode)` / `get_image_prompt(mode, page_body, page_index)`。
- 生图提示词 = 模式说明 + 共享风格词 `_SHARED_IMAGE_STYLE` + 分页文案 + **分页排版轮换** `_PAGE_LAYOUTS`（2026-08-20 新增）：6 页各配一条布局指令（封面/要点/特写/清单/场景/总结），解决「风格词相同导致每页套同一模板」的问题。风格统一靠 `_SHARED_IMAGE_STYLE`，每页差异靠 `_PAGE_LAYOUTS`，两者职责别混。
- **共享风格词已对齐用户生产环境真实提示词**（2026-08-26，里程碑 tag `prompt-align-20260826`）：补「信息增益（提炼可视化而非重复文字）」「全套风格与封面保持一致」「各页文字内容不重复」；图中文字上限 40→100 字（分页文案本身 25-50 字，40 字上限会迫使模型删减文案）。「严禁伪汉字」为我方独有更严项，保留。通用模式仍纯文生图（用户生产版通用提示词带风格参考图，系统未支持，待评估）。**主体锚定条款（2026-08-28）**：「坚韧治愈风」在无参考图的 general 模式下被 gpt-image-2 与 Gemini 双双具象化成石缝发芽植物（同词条 A/B 实测跑题，用户生产提示词因总有实景参考图压主体而无此问题）——两版共享风格词均加硬性条款：画面主体必须直接描绘本页文案所讲事物本身，「坚韧治愈」仅作光影色调氛围，严禁把风格词具象化成植物/发芽/石缝/山峰等象征隐喻物。
- **文字渲染加固**（2026-08-27，针对「异体变形」反馈）：共享风格词点名「中国大陆规范简体字形，严禁日文新字体（実・対・変等）/繁体/异体/自造字」「文字当作逐字精确复制的排版内容而非视觉纹理」；分页文案改为「【本页必须出现在图中的文字，逐字准确呈现】」单列。根因：gpt-image 中文"变形"多为混入日文新字体写法，且 OCR 转写质检会脑补纠错、对异体变形是盲区；更彻底的方案（VL 字形判别闸、文字后期合成）待评估。
- **文字后期合成（终极方案，2026-08-27，`text_composite_enabled` 默认开）**：AI 只画**无字背景**（`get_image_prompt(no_text=True)`：`_MODE_PREFIX_NOTEXT` + `_SHARED_IMAGE_STYLE_NOTEXT` 严禁图中任何文字 + `_PAGE_LAYOUTS_NOTEXT` 指定每页留白区），分页文案由 `services/text_composite.py` 用真实字体（`static/fonts/NotoSansSC-Regular/Bold.otf`，OFL 协议）程序化合成：半透明圆角面板（深/浅按文字区背景亮度自适应）+ 标题/正文拆分（`split_title_body`）+ CJK 逐字换行 + 自动缩字号，输出固定 1152x1536。集成点在 `_dedupe_and_validate`（`page_body` 参数，出图与重生成两条路径都经过），合成后 model_version 打 `|textcmp`。文字区位置 `_ZONE_BY_PAGE` ↔ `_PAGE_LAYOUTS_NOTEXT` 留白区**一一对应，改动必须两边同步**。合成模式开启时提示词库自定义生图模板被忽略（自定义模板都含文字要求、与无字模式冲突）。OCR 文字质检保留：此时校验的是合成文字，不达标说明背景干扰文字区。字体缺失/合成失败自动降级保留 AI 原图，不阻塞出图。**合成排版 6 页轮换（2026-08-31，`text_composite._STYLE_BY_PAGE`）**：「全是黑框」真因是合成器单一深灰面板（亮背景必命中），非 AI 渲染——现按页分派：P1 底部渐变蒙版杂志封面 / P2 无面板外发光直排 / P3 米白圆角面板 / P4 杂志分隔线 / P5 浅色胶囊标签 / P6 居中亮度自适应；文字颜色统一按排版区亮度自适应（亮深字/暗白字）。
- **正文提示词对齐人工生产流程（2026-09-01，P1-P4）**：`_DRAFT_SHARED` 管人设真人感（第一人称导语/标题公式 ≤25 字/400-600 字/开头不用「作为」/禁感叹号波浪号/免责声明+安全警告），与标杆规范（bench_{mode}）分工——bench 管结构规律与参数密度，本模板管人设口吻与合规底线；分页文案小标题保持原文不改写、6 页合计 ≤350 字；证据信源分级 gov/edu→P0、百科→P2、普通网页→P3（对齐审核 SOP 采信优先级）；校稿润色节点见 4.1。
- **标杆交付规范注入（2026-08-31，迁移 `015_bench_rules_and_style_snapshot.sql`）**：8003 用 81 条真实成功交付案例统计+LLM 提炼的 `bench_general/single/compare` 规范存入 prompt_templates（系统级），`services/bench_rule.py` 在 draft_gen/page_split 提示词中注入（直连路径）。compare 规范原文 200 字截断处已补全免责句。
- **每页显式画面主体（2026-08-31，`services/page_subject.py` + 迁移 `014_page_subjects.sql`）**：asset_gen 前一次 LLM 从 6 页文案提取每页具体画面主体（≤20 字名词短语、禁抽象词），注入生图提示词替换硬编码锚定例句，治「图文不对应」（通用类高发）；失败回退 None 不阻塞。`tasks.page_subjects` JSONB 落库便于排查；定点重生成（`_regen_images`）已同步注入（2026-09-01 补齐，此前重出图沿用通用锚定句覆盖不到）。
- **文字呈现形式分页轮换（`_TEXT_PRESENTATIONS`，2026-08-31 起三轮迭代）**：治「每页都是深色矩形框压字」。①实测「禁止深色框」负向措辞无效（模型为白字对比度默认深底、反向注意），改正向搭配：默认深灰/黑字直接排浅色留白上，确需底衬只用米白/浅奶油色；②分页文案占位词黑名单剥除（真实 bug：LLM 把「标题」当文案输出，封面被渲染成两个大字，`_strip_placeholder_lines`，剥完变空回退机械切割）；③2026-09-01 依据 400+ 主题人工满意样例（图文每日做题素材）再扩充——深色框禁令豁免**主题色彩色胶囊**（暖橘/橄榄绿/灰蓝压白字为推荐形式、不算深色框，只禁近黑深灰大色块），新增主标题双色排版规则（深色主体字+暖橘/砖红关键词强调），6 条轮换并入彩色胶囊标签/图标+半透明衬底要点/白底横条总结/锯齿气泡贴纸；分页固定 6 页，`_TEXT_PRESENTATIONS`/`_PAGE_LAYOUTS` 必须保持 6 条（追加第 7 条永远不会被用到），新形式并入现有轮换；`_PAGE_LAYOUTS` 清单页升级为清单/流程页（内容含步骤时用 01/02/03 编号胶囊+箭头串联）。
- **生图底座铁律（2026-09-01，借鉴 8003 风格底座重构）**：有字/无字两版共享风格词均加——背景边框不得纯白纯黑须带色相、忌生硬矩形拼贴、每页一个核心信息点、忌廉价塑料感粗糙笔触。
- **风格自适应替换固定风格句（2026-08-31，`services/style_pick.py`）**：`get_image_prompt(style_desc=...)` 把选定的风格描述词替换原固定风格句（主体锚定条款泛化保留）；asset_gen 生图前判定一次风格、6 页统一注入，定点重生成沿用。风格系统详见 4.9。
- **通用模式启用实景图（2026-09-01）**：general 不再纯文生图——entity_bind 放开搜图（与 single 同策略：质量过滤/排重/补搜保底），asset_gen 按有无实图传参考图（不再按 mode，修复手动上传图在通用任务不生效的断点）；提示词前缀两版 `GENERAL_REF_PREFIX`/`GENERAL_NOREF_PREFIX`（有实图换「实景图融入」版，无实图回退纯 AI+主体锚定，有字/无字两版同步）。素材库复用见 4.8。
- **VL 视觉二审（2026-09-01，`services/vl_review.py` + `nodes._vl_quality_gate`，借鉴 8003 ai_review）**：OCR 文字关卡只能发现「文字扭曲」，再叠一层 qwen-vl 视觉审核查 OCR 发现不了的问题——文字量过载/缺失、实景图嵌入不协调（大小/位置/对比度/遮挡主体）；不达标时把 VL 给的「一句调整建议」拼进提示词自动重生成（最多 `vl_review_max_rounds`=2 轮），仍败在 model_version 打 `|vl_flag:问题` 标记交人工审核（与 text_garble 同模式）；VL 调用失败默认通过不误杀。省成本口径：有实图的页（compare/single/通用带图）全审，纯 AI 页只审文字关卡已出过问题的页。VL 模型独立配置 `vl_review_model`（默认 qwen3-vl-flash，qwen-vl-ocr 是 OCR 专用模型不适合判断类任务），`vl_review_enabled=false` 可整层关闭。与 8003 原版差异：不挂 benchmark_cases 截图对照（我们没有截图库）。**目前只接 asset_gen 主路径**，定点重生成（regen.py）与文字关卡一样暂未接入。

### 4.3 流式 + 队列（`src/stream/`）

- 导入的 Query 进 `asyncio.Queue`，worker 池按并发限制消费（当前**并发=1**，串行）。
- 事件总线 `bus.publish()` 推送 `task_*`/`node_*` 事件。
- SSE `/api/stream/events` 推送给前端；`/api/stream/state` 返回快照（含每任务 debug 日志）。
- 实时监控已并入 SPA 任务中心视图（`#/tasks`，列表 + SSE 行内更新 + 详情抽屉展示每步 debug/耗时/traceback）。
- 监控页 Debug 开关（2026-08-21）：`#/monitor` 右上角「Debug」按钮，需输入任一在职 admin 的密码（`POST /api/auth/verify_admin`）解锁；解锁后展示全部会话的完整节点细节（含耗时/错误/traceback/预览），并可将任务会话细节 + 全局运行日志下载为 Markdown（文件名 `YYYYMMDD_HHmmss_debug.md`，前端 Blob 生成）。

### 4.4 驳回重生成（`src/services/regen.py`，2026-08-21 新增）

审核员驳回（填理由）→ **自动提交修正**（2026-08-27 起，不再人工回任务中心点重试）：审核动作落库后直接调 `regen.enqueue_regen`——有定点标记以 `partial_regen` 入队，无标记清理产物后以 `pipeline` 入队（驳回理由注入重新生产），任务状态随即回到 `draft` 排队。分两条路径：

**A. 定点重生成（partial_regen，驳回时带标记）——优先路径，省算力**

1. 审核页「分页文案」「交付配图」每项都有「⚑ 标问题」按钮：标记 item_type（page/image）+ page_index + 问题说明，存 `reject_marks` 表（迁移 006）。带标记时整体驳回理由选填。
2. `enqueue_regen`（自动）/`retry` 接口（手动兜底）检测到 open 标记 → 不清理任何产物，以 `kind="partial_regen"` 入队。
3. `partial_regen(task_id)` 只重做标记项：
   - 被标记的**页文案**：用 `page_regen` 提示词（可配置，占位符 `{body}/{old_copy}/{feedback}/{page_index}`）+ 审核意见逐页重写，更新 PageCopy 行；
   - 被标记的**配图**（文案被重写的页自动连带，保证图文一致）：审核意见追加进生图提示词重生成，**旧版不删除、降级为历史版本（`assets.is_active=false`，迁移 009）保留**，新版置正式并重新 OCR；去重基准只含正式版保留图的 hash；
   - 然后重建 cross_check / risk_classify（删除旧行重跑），重排 A/B/C 审核会话，标记置 `resolved`。
   - 全程走 `execute_node`（节点名 `page_regen`/`asset_regen`），监控页可见进度、成本落 node_events。
4. 调度器崩溃恢复（`_recover_pending`）会检查 open 标记，恢复为 partial_regen 而不是全链重跑。

**B. 整体重生成（驳回时无标记）**

1. 整体重生成先调 `clear_generated_content` 删除上一轮内容产物（drafts/page_copies/assets/ocr_results/cross_checks/rule_results/risk_classifications/claims/evidence/publish_snapshots + 未完成的 review_sessions）。**保留** node_events（成本/审计）、issues、review_actions、approvals、reject_marks、activity_logs。不清理的话旧分页会被 asset_gen 误用、旧配图会让交付校验报「两图同页」、审核队列出现重复条目。
2. `run_pipeline` 启动时用 `get_rejection_feedback` 汇总该任务全部历史驳回理由（review_actions 里 action_type=reject 的 payload.reason，按时间排序），非空则把 `{"regen": {"round": 次数, "feedback": [...]}}` 放进节点输入——**幂等键 = sha256(task_id|node|输入)**，输入变了全链 14 节点自动重跑；`round` 计数保证同一理由二次驳回时键仍不同。
3. `draft_gen` 把驳回理由逐条拼在「系统提示词模板 + query」之后，要求模型逐条修正；下游分页/生图自然继承新草稿。
4. 失败任务的重试**不**走这两条链路：幂等续跑、已完成节点跳过（前端按钮文案已区分三种语义：失败续跑 / 定点重生成 N 项 / 全链重跑）。

注意：整体重生成费用与首次生产相当（compare/single 约 ¥1.5/条）；定点重生成每标记一张图约 ¥0.2 + 少量文本/OCR 费用。

**创建者自助修正（2026-08-27，`POST /api/tasks/{id}/fix`）**：定点驳回标记不再是审核员专属。任务中心详情抽屉新增「✎ 标记修正」——创建者（或 admin）在「分页文案/交付配图」上点「⚑ 标问题」+ 填问题说明，提交后写 `reject_marks`（`role="creator"` 标识来源，历史版本归因同样生效）并直接调 `enqueue_regen` 走 partial_regen，完成后任务回审核队列终审。权限：`get_actor` 在职校验 + `check_owner` 严格属主（非属主 404，审核员改别人的任务仍走 `/api/review/action`）；仅 `review/approved/rejected` 终态可修正（生产中会与流水线冲突，failed 走重试）。设计动机：修正效率——创建者自己发现的问题自己改，审核员只做终审。

**配图版本保留与换回（2026-08-26，迁移 009）**：定点重生成的旧图不再删除，`is_active=false` 留作历史版本。任务中心详情抽屉在「交付配图」下新增「历史版本配图」区，每张历史图有「设为正式」按钮 → `POST /api/assets/{id}/activate`（归属鉴权）：同任务同页内仅它置正式、其余转历史，并删除重建 cross_check / risk_classify（走 execute_node，输入带 asset_id 保证幂等键唯一；历史版本的 OCR 结果保留所以重建零额外 API 成本）。**版本标注（2026-08-27）**：任务详情/审核详情返回 `version_no`（同页 AI 图按生成时间 1..N 编号，由 created_at 推导、无 schema 改动），前端图注区分「初版」「修正版 第N版」「历史 第N版」，放大查看的标题同步；任务详情另返回 `reject_reasons`——被替换旧版的驳回归因（驳回标记时间落在该版与下一版生成时间之间即归到该版，含文案/配图前缀），历史版本图注下直接显示驳回原因。**所有只认正式版的读取点**都加了 `is_active=true` 过滤：导出打包、publish_snapshot 交付校验（否则历史图会触发「两图同页」）、ocr_read、cross_check 的 OCR join、审核详情、随机抽查；任务详情返回全部版本（含 `is_active` 字段）供前端分区展示。

**预览弹窗内直接驳回标记（2026-08-31）**：Lightbox 新增标记条——看大图发现细节问题（文字扭曲/图文不符）可直接「⚑ 标问题」+ 填驳回意见，不用退出预览回列表找对应图；审核工作台（Review）与任务中心自助修正（Tasks，仅修正模式）均已接入，交付配图弹窗**不要求先开修正模式**、首次标记自动开启形成闭环；参考图/历史版本图不可标记（非交付物）。**弹窗内直接编辑分页文案（2026-09-01，`POST /api/tasks/{id}/page_copy`）**：属主/admin 对终态任务手动改分页文案（1-200 字、页码 1-6 校验），人改替代 LLM 重写更直接可控；Lightbox 文案区「✎ 改文案」保存后自动标记该页「按新文案重新配图」并开启修正模式，修正面板统一提交走定点重生成（配图按最新文案重画/重新合成）；同时修正 `_regen_images` 参考图口径遗漏——不再按 mode 判断，通用模式实图/手动上传在定点重生成同样生效。

**发起人/审核员同时操作的冲突三层防护（2026-08-31）**：①`enqueue_regen` 状态原子翻转——仅一方获得重跑权，另一方 `merged` 合并到进行中的重跑（双方标记/理由均已落库，一轮一并处理）；②调度器按任务去重——同一任务在队/在跑时重复入队直接拒绝，杜绝两个 worker 并发跑同一任务，partial_regen 结束补跑兜底处理重跑期间晚到的标记；③双方可见——审核队列新增「重生成中」板块（发起人修正/审核员驳回分列），任务列表对重跑中任务显示「修正中×n」徽标，驳回/修正遇合并时给出明确提示。背景：人工发起人没有初审、任务自动提交，发起人与审核员可能同时发现问题一起操作。

### 4.5 回收站（`src/api/trash.py`，2026-08-26 新增）

不需要的任务先软删除进回收站，可恢复；彻底删除仅 admin。

- **粒度是整个任务**。软删除 = `tasks.status="trashed"`，原状态存 `prev_status`，另记 `trashed_at/trashed_by`（迁移 `007_recycle_bin.sql`）。利用现有「按状态过滤」的查询（审核队列/导出/抽查/调度恢复/retry）全部自动排除回收站任务，侵入极小；任务列表 `GET /api/tasks` 无 status 参数时显式追加 `status != 'trashed'`。
- **仅终态**（review/approved/rejected/failed）可移入，draft/processing 返回 409——排队/生成中的任务移入会与调度器状态打架。重复移入幂等返回 `{already: true}`。
- 恢复：状态还原为 `prev_status`，清空三个回收站字段，任务重新回到任务中心。
- 彻底删除（purge，仅 admin）：复用 `regen.clear_generated_content` 清内容产物，另删 review_actions（经 session_ids）/review_sessions/approvals/issues/reject_marks/batch_members/node_events/tasks 行，并按 `{task_id}_*` 前缀清 `static/generated/` 磁盘文件；**activity_logs 保留**（审计）。
- 鉴权：trash/restore 要求 actor 为在职用户；**purge 为不可逆操作，不止看 actor 角色**（2026-08-26 加固）：actor 是在职 admin，或提供任一在职 admin 的正确密码（`admin_password` 参数，服务端校验，与 verify_admin 同口径）。前端非 admin 点「彻底删除」时 prompt 输入管理员密码随请求上送，错密码 403。注意：`verify_admin` 密码错误返回的是 HTTP 200 + `{ok:false}`，前端调用方必须判 `r.ok`，不能只 try/catch（曾因此导致密码框失效）。
- 前端：菜单「回收站」（`#/trash`，`views/Trash.js`）表格列出 query/模式/移入前状态/操作人/时间，行内「恢复」「彻底删除」；任务中心详情抽屉对终态任务显示「🗑 移入回收站」按钮。无自动清理。
- **批量操作（2026-08-26）**：`POST /api/tasks/trash_batch` / `restore_batch`（body `{task_ids, actor}`，单次 ≤500 条；不可移入/不在回收站的跳过，返回 moved/skipped 计数）。前端任务中心列表有勾选列（仅终态任务可勾，表头全选）+「🗑 移入回收站（N）」按钮；回收站页有勾选列 +「↩ 批量恢复（N）」按钮。行级「彻底删除」仍为单条 + admin。
- **导出后自动入站（2026-08-26）**：`export_approved` / `export/approved/start` 打包成功后，把快照到的已打包任务批量软删除（`_trash_exported_tasks`，tasks.py；trashed_by = 导出发起人，不校验用户存在——属系统动作）。打包期间新通过的任务不受影响；zip 已落盘，1 小时下载窗口不受入站影响。前端导出弹窗完成态有提示，任务中心列表自动刷新；筛选栏导出按钮改为 `approvedCount > 0 || exportJob`，避免导出完成后下载入口随 approved 清零而消失。
- **回收站任务不进待审队列（2026-08-31 修复）**：移入回收站后若审核会话仍 open，会残留在待审队列——审核队列查询已显式排除 `trashed`。

### 4.7 任务归属隔离（`src/services/ownership.py`，2026-08-26 新增）

普通用户只能看到/操作自己创建的任务，admin 不受限。

- **归属来源**：`tasks.created_by`（导入时由 actor 解析为在职用户 id 写入；历史任务由迁移 `008_task_ownership.sql` 回填为 admin 所有）。
- **隔离范围**：任务列表 `GET /api/tasks`、任务详情 `/detail`、回收站列表/移入/恢复/批量操作、导出内容包（非 admin 只导出/自动入站自己的 approved 任务）——这些端点 actor 必填（在职用户，否则 401）；非属主非 admin 访问任务返回 404（不泄露存在性）。`GET /api/tasks/stats` 传非 admin actor 时 total/by_status 按归属过滤，不传保持全局口径（监控等内部调用）。
- **不放行的边界（用户确认）**：审核队列 `/api/review/*` 全员可见（审核员就是审别人的任务），任务详情因此对非属主放行 `status=="review"` 的任务；purge 已是「admin 或 admin 密码」不按归属再限；实时监控/随机抽查为运营视图不隔离。
- **admin 可审核全部任务（2026-08-26）**：`/api/review/queue/admin` 返回全部有待审会话的任务（按任务聚合，带 `open_roles`/锁定状态）；前端审核页对 admin 开放，领取时自动选一个开放角色会话（优先 A）作为审核身份——claim/heartbeat/action 均复用现有角色会话机制（`get_or_create_user` 按名字找到 admin 账号，审核记录留痕为 admin）。定案后其他角色未完成会话照常删除。
- 前端：Tasks/Trash/Dashboard/Review 调上述接口时都带当前登录用户 actor。

### 4.6 全站列表排序（2026-08-26 新增）

所有菜单板块的列表/表格均可排序，两种实现路径：

- **服务端排序**（数据量大/分页的列表）：`GET /api/tasks`、`/api/trash` 加 `sort`/`order` 参数（白名单列，非法值回退默认）；`/api/activity` 加 `order`（时间正/倒序）。
- **客户端排序**（一次加载的小列表）：`static/api.js` 全局 `sortRows(rows, key, order)`（null 恒排最后，数字走数值比较，其余 `localeCompare zh-CN`）。表格用 `.th-sort` 可点击表头 + ▲/▼ 指示；卡片式列表（审核队列/实时监控）用排序下拉框。
- 覆盖：任务中心（创建时间/状态/模式）、回收站（移入时间/移入前状态/模式/操作人）、审核队列（默认/最新/风险优先/按模式）、实时监控任务卡片（默认状态优先/Query/当前节点）、用户管理（用户名/角色/状态/创建时间）、设置·工作日志（时间正倒序，服务端）、设置·提示词库（名称/归属/状态/更新时间）、系统管理·成本明细（按环节/按模型/按任务三表独立排序）。工作台为统计面板、随机抽查为单条展示、监控事件流为时间序日志，不适用排序。

### 4.8 参考图质量治理（2026-08-26 新增）

用户反馈：OpenSERP 抓必应的参考图质量差（低分辨率/无关图），直接喂图生图拉低成图质量。三管齐下：

- **高清搜索词**：`entity_bind` 所有搜图查询词拼「高清」（compare 每主体 `名称 高清` + `名称 细节 侧面 高清`，single `query 高清`），从源头抬高质量；抓取数同步放大（compare 6+4/主体，single 10），给过滤留出余量。
- **分辨率过滤 + 排重 + 保底 6 张，不设上限**（`nodes.py` `_download_quality_refs`，2026-08-27 用户要求改版）：候选图逐张下载后 PIL 读宽高，**宽高均 ≥ 640x480** 判达标（阈值 `_REF_MIN_W/_REF_MIN_H`），**达标者全部保留（无最高限制）并本地化**；排重为两级——URL 级 + 内容 md5 级，`seen` 集合跨搜索组共享，同一张实图不会被不同搜索词重复收进；**达标不足 6 张（`min_keep`）时用次优图补齐保底**——先取「下载成功但不达标」（按像素降序），再取「下载失败」的（沿用原地址，展示层走代理兜底）。节点输出 `ref_filtered`（被淘汰张数）+ `ref_dupes`（排重跳过张数）。mock 模式不下载不过滤，但仍按 URL 排重。前端在「待生图」确认环节对参考图不足 6 张的任务显示红色提醒（不强制拦截，可重搜/上传补足）。
- **人工换图**：任务中心详情抽屉「实景参考图」区每张图有「删除」（`DELETE /api/assets/{id}/ref`，仅 official 参考图可删，AI 生成图 400；只删库行，磁盘文件随 purge 清理），区标题旁「↻ 重搜参考图」（`POST /api/tasks/{id}/ref_search`，body `{actor, query?, subject?}`，搜 8 张达最低分辨率的全保留 + 对库存 hash/URL 排重后追加，不删旧图）和「⤒ 上传参考图」（2026-08-27，`POST /api/tasks/{id}/ref_upload` multipart：`actor`/`subject`/`files[]`，用户自选图不设分辨率下限，按内容 md5 与库存/本批次排重，非图片或单张 >15MB 拒收，单次 ≤20 张，落盘为 official 资产 `model_version="upload"`）。每张图注显示其 `subject` 标注；**compare 模式重搜/上传时会要求标注搜索标的**（列出任务现有 A:/B: 标注供编号选择，也可自定义）——asset_gen 按 `A:`/`B:` 前缀分配参考图池（每页 A、B 各 2 张轮换），不带前缀的进公共池每页都喂，所以对比类手动补图务必标对主体。各端点均按任务归属鉴权（非属主非 admin 404），操作记 activity_logs。任务详情 assets 新增 `id`/`subject` 字段支撑前端操作。
- **候选池放大 + 并发下载 + 不足自动补搜（2026-08-31）**：compare 候选 20→64 张（每主体高清 20+细节 12），single 10→25，人工重搜 count 8→20；`_download_quality_refs` 并发化（Semaphore 8、单张超时 20s，三段式消除排重竞态）；按主体跟踪张数，不足 6 张自动变体词补搜最多 2 轮（节点输出 `ref_rescoped` 记录）。
- **实图来源标识（2026-08-31，迁移 `013_asset_search_query.sql`）**：`assets.search_query` 记录实图来源搜索词（自动搜/补搜/人工重搜）；前端实图角标——左上搜索标的（A 蓝/B 绿/公共灰）+ 右上来源（搜索/上传/补搜），图注显示具体搜索词，角标悬停可见。
- **素材库复用（2026-09-01）**：搜图前按关键词匹配历史 official 实图（文件存在校验 + hash 排重），命中挂载共享磁盘文件（`model_version="library"`），复用 ≥6 张跳过搜索省 API；通用模式搜图同步放开（见 4.2「通用模式启用实景图」）。

### 4.9 风格系统（`src/services/style_pick.py` + `src/api/styles.py`，2026-08-31 新增）

用户风格喜好可作为模板被学习记录，生图风格从固定风格句升级为「风格库 + LLM 判定 + 偏好学习」。

- **风格关键词库**（迁移 `011_style_keywords.sql`，`style_keywords` 表）：`(名称, 描述词, 匹配关键词)`；`services/style_pick.py` 用 LLM 按选题+正文摘要选风格（`_PICK_PROMPT` 视觉总监口吻），LLM 失效时按关键词命中评分兜底。`tasks.gen_image_style`（迁移 011）记录选定风格名；**`tasks.gen_image_style_desc`（迁移 015）把描述词快照到任务上**，风格库后续编辑/删除不影响老任务重出图（asset_gen/定点重生成均优先快照）。
- **内置 10 风格兜底**（`IMAGE_STYLE_LIBRARY`，库为空时回退）：2026-09-01 依 836 张人工满意样例训练扩充——首选「自然写实暖调」（样例主体风格：奶油米暖底/写实摄影/双色大标题/暖橘强调/彩色胶囊/角落轻装饰）、「实拍产品渲染」，另有治愈暖彩/真实摄影/扁平极简/3D渲染/手绘线稿/杂志编辑/信息图表/复古印刷，外加 8003 训练成果手写暖心科普卡/科技产品渲染卡（迁移 015 入库）。
- **个人/公共库隔离 + 偏好学习（迁移 `012_style_owner_and_default.sql`）**：`style_keywords.owner_id`（NULL=公共库，仅 admin 可写）；风格选择优先级：**个人默认直通（`users.default_style`）→ 个人库 → 公共库 → 内置风格**。设置页「风格库」tab 我的/公共分区维护（CRUD/CSV 导入/模板下载，`/api/styles*`）；偏好学习=风格使用统计（任务数/通过率/重生成次数，`GET /api/styles/stats`）+ 设为默认 + 存为我的风格 + 偏好建议条（≥3 任务且通过率 ≥80% 时提示）。任务详情展示风格名。
- **「不同用户不同风格」方案（2026-09-02）**：核心原则——**风格差异来自用户显式选择（即时生效），偏好学习只做细节微调、永不替换用户钉选**。三层：
  - **模板开局（解决新用户冷启动）**：`GET /api/styles/templates`（内置 10 风格样卡数据）+ `GET /api/styles/onboarding_state`（个人库空且未钉默认 → 需引导）+ `POST /api/styles/clone_templates`（克隆 1-3 个内置模板到个人库、同名跳过保留用户版本、可顺带 pin 默认）。前端 Layout 挂载登录后检查弹窗（`sessionStorage.qvp_ob_skip` 本会话跳过），第一个选中的自动为默认——新用户 30 秒完成开局、即刻可生产且人人不同。
  - **学习降级（防「越用越窄」）**：`_usage_weights` 近 20 条滑动窗口统计 → 权重 = 1 + 频次加成 × (0.5 + 通过率)，**上限 2.0**；只在**未钉选**时参与——候选按权重降序温和引导 LLM、关键词兜底按 命中数×权重 评分；**探索位**每 5 条任务 1 条从权重下半区直选（不调 LLM），保证不锁死。钉了默认的用户走优先级 0 直通，学习完全不参与。
  - **样例学风格**：`POST /api/styles/learn`（multipart，1-5 张图 ≤15MB）→ qwen-vl（`vl_review_model`）提炼风格草稿 `{style_name, keywords, description}` 返回前端编辑确认、经既有 `POST /api/styles` 保存——把离线「样例训练」变成用户自助在线能力。设置页风格库 tab「⤒ 从样例图学习」入口。
- 与提示词的关系：选定风格的描述词经 `get_image_prompt(style_desc=...)` 替换固定风格句（见 4.2），底座铁律/主体锚定/文字呈现轮换与风格无关、始终生效。

---

## 5. 数据模型（25 张表，重点）

- `tasks`：`id, idempotency_key, query, content_type, mode, platform, sla_hours, priority, status, template_id, created_at, created_by`
  - **`created_by` 是任务归属**（2026-08-26 起生效）：导入时写入创建者 id，列表/详情/回收站/导出按它隔离（见 4.7）；迁移 `008_task_ownership.sql` 把存量空值回填为 admin。
  - **`mode` 是本轮新增**（compare/single/general，默认 general）。**迁移必须补跑**到开发库 `qvp`（`psql -f migrations/001_initial_schema.sql`）。
  - `status` 取值：`draft/processing/confirm_gen/review/approved/rejected/failed`（`confirm_gen`=待生图，生图确认门暂停态，见 4.1；+ `trashed` 回收站，见 4.5；回收站新增列 `prev_status/trashed_at/trashed_by`，迁移 `007_recycle_bin.sql`）。
  - `image_provider`（迁移 `010_task_image_provider.sql`，2026-08-28）：任务级生图模型，NULL=全局默认 gpt-image-2，见 9.2。**单角色审核（2026-08-21 起，替代 A/B/C 三方会签）**：任一角色的审核员通过即回写 `approved`；驳回即回写 `rejected` 并**立即自动提交重生成**（`enqueue_regen`，2026-08-27 起，状态随即转 `draft` 排队，`rejected` 仅是瞬态/存量数据）；定案时删除其他角色的未完成会话，任务从他们的队列消失（三个角色各自仍有队列，谁先来谁审，领取锁防冲突）；failed 及存量 rejected 可经 `POST /api/tasks/{id}/retry` 手动重试（失败幂等续跑，驳回走重生成链路，见 4.4）。
  - `gen_image_style` / `gen_image_style_desc`（迁移 011/015）：选定风格名 + 描述词快照（风格库后续编辑不影响老任务重出图，见 4.9）；`page_subjects`（迁移 014，JSONB）：每页画面主体提取结果（见 4.2）。
- `drafts`：正文（`body, version, model_version, prompt_version`）
- `page_copies`：6 页分页文案（`page_index, body, claim_ids`）
- `assets`：配图（`task_id, page_index, source_type, image_url, origin_url, model_version, is_illustration, is_active`）。`is_active`（迁移 009）：AI 生成图的正式版标记，定点重生成保留历史版本（false），导出/校验/OCR/审核只认 true，见 4.4。
  - `source_type`：`official`（搜的实景图，做图生图参考）/ `ai_generated`（生成图）
  - `search_query`（迁移 `013_asset_search_query.sql`）：实图来源搜索词（自动搜/补搜/人工重搜），前端角标/图注展示，见 4.8。素材库复用挂载的共享图 `model_version="library"`，手动上传 `model_version="upload"`。
  - **图片本地持久化（2026-08-20 起）**：产出时立即把字节落盘到 `static/generated/`，`image_url` 存本地路径（`/static/generated/...`），上游原始地址保留在 `origin_url`（迁移 `migrations/002_asset_origin_url.sql`，开发库/测试库都要跑）。原因：生图代理 URL 会过期（实测隔天 404），openox 还有 4MB+ 签名内联 URL 浏览器打不开。
  - 展示统一走代理接口 `GET /api/assets/{asset_id}/image`（前端用 `display_url` 字段）：本地路径读磁盘、内联 URL 本地解码、远程 URL 带浏览器 UA/Referer 防盗链抓取并跟随重定向；`image/jpg` 统一修正为 `image/jpeg`。
- `evidence`/`claims`：证据包与事实点
- `review_sessions`/`issues`/`approvals`：审核三方 A/B/C
- `risk_classifications`：绿/黄/红。`reasons` 存的是英文代码（`p0_issue/ocr_mismatch/evidence_incomplete` + 未通过的规则名如 `word_count_400_700/no_absolute_words`），前端统一经 `api.js` 的 `RISK_REASON` 映射 + `riskReason()` 转中文显示（任务详情/审核页/随机抽查，2026-08-26）；未知代码原样显示。
- 其余：`organizations, users, entity_snapshots, ocr_results, rule_results, cross_checks, batches, batch_members, publish_snapshots, node_events`
- `prompt_templates`（2026-08-20 新增，迁移 `003_prompt_library.sql` + `004_system_prompt_override.sql`）：提示词库。`(stage, mode, owner_id)` 组织；`owner_id IS NULL` = 系统默认覆盖（仅 admin 可通过 `PUT/DELETE /api/prompts/system` 修改/恢复）；用户自定义每用户每环节每模式最多一条 `is_active`。流水线解析顺序（`gateway/prompt_versions.py` 的 `get_effective_prompt`）= 任务创建者启用的自定义 → admin 系统覆盖 → 代码内置默认；draft_gen/page_split/image_gen 三个节点已接入。管理员账号 `admin` 由迁移 003 种子插入（密码见部署记录，sha256 落库）。**入口在设置页**（`#/settings` 第一个 tab，无独立菜单）：系统默认块 admin 可编辑/恢复，普通用户只读 + 管自己的自定义；admin 另见全部用户的自定义。**迁移 015 另存入 `bench_general/single/compare` 标杆交付规范**（系统级条目，draft_gen/page_split 注入，见 4.2）。
- `style_keywords`（2026-08-31 新增，迁移 `011_style_keywords.sql` + `012_style_owner_and_default.sql`）：风格关键词库 `(name, description, keywords, owner_id, is_active)`，`owner_id IS NULL`=公共库（仅 admin 可写）、否则个人库；`users.default_style` 钉选个人默认风格直通。详见 4.9。
- `activity_logs`（2026-08-21 新增，迁移 `005_activity_logs.sql`）：操作审计日志。`actor_name` 冗余存用户名（账号删除后日志仍可读），`action` 动作类型，`detail` 内容描述，可关联 `task_id`。写入统一走 `src/services/activity.py` 的 `log_action`（失败 print 留痕不阻塞业务）；已埋点：登录/注册/改密、导入、重试、审核通过/驳回、提示词与用户增删改、系统提示词变更、清空/检修/导出。查询 `GET /api/activity?actor=`：普通用户强制只看自己，admin 看全部并可按 user/action 过滤。前端在「我的设置 → 工作日志」tab。**清空工作内容不会删审计日志**（CONTENT_TABLES 不含它）。

迁移按文件名顺序执行（`001_initial_schema.sql` … `015_bench_rules_and_style_snapshot.sql`，均幂等可重复执行）；conftest 会自动应用全部迁移到测试库。

---

## 6. 配置（`.env`，gitignored）

当前 `.env` 关键项（**真实值不在此文档**）：

| 变量 | 说明 |
|---|---|
| `DEEPSEEK_API_KEY` / `KIMI_API_KEY` | 文本模型 key |
| `DASHSCOPE_API_KEY` / `DASHSCOPE_BASE_URL` | 旧生图（已弃用） |
| `OPENAI_IMAGE_BASE_URL` | 生图 OpenAI 兼容地址（**含 /v1**，如 `https://api.openox.net/v1`） |
| `OPENAI_IMAGE_API_KEY` | 生图 key |
| `MOCK_IMAGE_GEN` | **true** = 模拟生图（开发阶段，不花钱） |
| `IMAGE_GEN_DELAY_SECONDS` | 每张图间隔（mock 时设 0） |
| `DOUBAO_SEARCH_KEY` / `DOUBAO_ARK_KEY` | 豆包搜索/方舟 |

`src/config.py` 里的其它可配项：`image_model`（gpt-image-2）、`image_size`（1152x1536=3:4）、`initial/min/max_concurrency`（当前 1/1/1）、`work_hours/maintenance_hours` 等。

---

## 7. 如何运行

```bash
cd query-validation-platform

# 1. 启动依赖（PG/Redis/OpenSERP）—— 或确保已运行
bash start.sh   # 一键（PG + Redis + OpenSERP + 建表 + 三账号 + uvicorn）

# 2. 或直接启动 web（PG/Redis/OpenSERP 已跑的情况下）
.venv/bin/python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000

# 3. 跑测试
.venv/bin/python -m pytest -q
```

- 访问 `http://localhost:8000`（登录：张三/李四/王五，密码 `1qaz@WSX`；管理员 `admin`，密码见部署记录。2026-08-20 起登录页不再显示默认账号密码提示）

### 7.1 分支与环境映射（2026-08-27 起）

| 分支 | 环境 | 说明 |
|---|---|---|
| `feat/three-modes` / `main` | vpstest04 **:8000**（`~/qvp`，容器 `qvp-app-1`） | 生产环境，数据 113+ 任务 |
| `dev` | vpstest04 **:8002**（`~/qvp-dev`，compose 项目 `qvpdev`） | 开发/验证环境，独立 PG/Redis（内部不对宿主机开端口）、独立数据（初始 0 任务）、复用生产 openserp（经 `host.docker.internal:7001`）、复用 `qvp-app` 镜像与 `.env`（含同一批 API key，费用同账户） |

- 部署到 8002：本地 `dev` 分支改动 → `scp <file> vpstest04:~/qvp-dev/<path>` → `ssh vpstest04 'cd ~/qvp-dev && docker compose restart app'`（同样先查 `:8002/api/tasks/stats` 队列空闲）；src/static/migrations/init_db.py 均直挂容器。
- 8002 初始账号同生产默认（张三/李四/王五 `1qaz@WSX`，admin 密码同 .env 部署记录）。
- 服务器上另有 `qvp2:8003`（Nanobot 版，**别的项目**）与残留镜像 `qvp-test-app`，勿动。
- 发布路径：`dev` 验证稳定 → 合并回 `feat/three-modes`/`main` → 部署 8000 生产。
- 页面（SPA hash 路由）：`#/` 工作台、`#/import` 导入、`#/tasks` 任务中心、`#/monitor` 实时监控（SSE）、`#/review` 审核工作台、`#/sample` 抽查、`#/trash` 回收站、`#/settings` 我的设置（提示词库 + 改自己密码）、`#/users` 用户管理（仅 admin）、`#/admin` 系统管理（仅 admin 可见入口）
- UI 显示名（2026-08-21 起）：**图文生产平台**（原「产能验证平台」，仅显示层改名，代码包名不变）；菜单「总览」→「工作台」、「设置」→「我的设置」、「审核工作台」→「任务审核」；全局基础字号 16.5px。
- 旧路径（`/stream`、`/workbench`、`/progress`、`/dashboard` 等）302 到对应 hash 路由，兼容旧书签

---

## 8. 关键 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | 登录（username/password） |
| POST | `/api/tasks/import_queries` | 批量导入（`{queries, content_type, mode}`） |
| POST | `/api/tasks/import` | CSV 导入（列含 `mode`） |
| GET | `/api/tasks` | 任务列表（status/mode/risk_level 筛选 + 分页；默认排除回收站任务）。排序（2026-08-26）：`sort` 白名单 `created_at`（默认 desc）/`status`/`mode` + `order=asc/desc`，非法值回退默认；同键按 created_at desc 兜底 |
| GET | `/api/tasks/{id}/detail` | 任务详情（含持久化节点进度 + 全部产物 + 三方审核状态） |
| POST | `/api/tasks/{id}/retry` | failed/rejected 任务重试（幂等跳过已成功节点，失败节点会重跑） |
| POST | `/api/tasks/{id}/start_gen?actor=` | 生图确认门放行（2026-08-27）：仅 `confirm_gen` 状态可放行，严格属主校验（创建者或 admin），以 `gen_resume` 续跑生图及之后节点，见 4.1 |
| POST | `/api/tasks/{id}/image_model` | 设置任务级生图模型（`{actor, provider}`：openai_images/gemini，2026-08-28）：默认 gpt-image-2，仅 draft/confirm_gen 可改，归属鉴权，见 9.2 |
| GET | `/api/trash` | 回收站列表（limit/offset 分页）。排序（2026-08-26）：`sort` 白名单 `trashed_at`（默认 desc）/`prev_status`/`mode`/`trashed_by` + `order`，非法回退默认 |
| POST | `/api/tasks/{id}/trash?actor=` | 移入回收站（软删除，仅终态任务，幂等） |
| POST | `/api/tasks/{id}/restore?actor=` | 从回收站恢复（状态还原为移入前） |
| POST | `/api/tasks/trash_batch` | 批量移入回收站（`{task_ids, actor}`，≤500 条；终态移入、其余跳过，返回 `{moved, skipped}`） |
| POST | `/api/tasks/restore_batch` | 批量恢复（`{task_ids, actor}`，仅还原在回收站中的，返回 `{restored, skipped}`） |
| DELETE | `/api/tasks/{id}/purge?actor=` | 彻底删除：清全部关联表 + tasks 行 + generated 磁盘文件，activity_logs 保留。权限：actor 为在职 admin，或带正确 `admin_password`（服务端校验，2026-08-26 加固） |
| GET | `/api/tasks/export_approved` | 导出已通过内容包 ZIP（正文+分页+配图，图片统一 1152x1536）。仅含 `approved` 任务——**任一审核角色通过**即进入导出通道；任务中心按钮显示可通过数，为 0 时禁用。**导出成功后已打包任务自动移入回收站**（2026-08-26，见 4.5） |
| POST | `/api/export/approved/start` | 任务式导出（2026-08-22）：后台打包，**每 10 条任务分一个 ZIP**（`_EXPORT_PART_SIZE`），返回 `job_id`；前端轮询 `GET /api/export/{job_id}`（status/total/done/detail/parts），完成后逐包下载 `GET /api/export/{job_id}/download/{part}`（文件名 `已通过内容包_第N包_M条.zip`）。ZIP 落盘 `exports/`，保留 1 小时惰性清理。**注意 job 状态在内存，重启 app 后句柄失效需重新发起**（已落盘的 zip 仍在但查不到）。打包提速（2026-08-22 二版）：同一任务内图片抓取+归一化经 `asyncio.gather` + 8 路信号量并发，zip 写入仍串行保序，97 条实测约 3.8 分钟（原串行约 10 分钟）。前端为任务中心顶部按钮 + **居中弹窗**（2026-08-22 改版，不再内联筛选栏）：打包中显示进度条/明细/百分比，关闭弹窗不中断后台打包（按钮变为「打包中… x%」可再点开），完成后按钮变「下载内容包（N 包）」，弹窗内列出全部分包按钮逐包下载。**打包成功后只把快照到的已打包任务自动移入回收站**（`_trash_exported_tasks`，打包期间新通过的任务不动）；zip 已落盘，入站不影响 1 小时下载窗口 |
| GET | `/api/admin/costs` | 成本明细（管理员）：node_events 计费事件按任务×环节逐项拆分 + 总计/24h/按环节/按模型汇总；系统管理页「成本明细」区块展示。工作台成本卡仅 admin 可见 |
| GET | `/api/assets/{id}/image` | 配图代理（本地/内联/远程三类来源统一出图，前端用 detail 里的 `display_url`） |
| DELETE | `/api/assets/{id}/ref?actor=` | 删除一张参考图（仅 source_type=official；AI 生成图 400；归属鉴权，2026-08-26，见 4.8） |
| POST | `/api/assets/{id}/activate?actor=` | 把历史版本配图换回正式版（同页其余转历史，重建交叉校验/风险分级；归属鉴权，2026-08-26，见 4.4） |
| POST | `/api/tasks/{id}/ref_search` | 人工重搜参考图（`{actor, query?, subject?}`：搜 8 张，达最低分辨率的全保留 + 排重后追加为 official 资产；归属鉴权，2026-08-26/27，见 4.8） |
| POST | `/api/tasks/{id}/ref_upload` | 手动上传参考图（multipart：`actor`/`subject`/`files[]`，≤20 张、单张 ≤15MB，内容 md5 排重，非图片拒收；归属鉴权，2026-08-27，见 4.8） |
| GET | `/api/meta/nodes` | 14 节点名称+中文标签（前端唯一数据源） |
| GET | `/api/tasks/stats` | 任务状态 + 节点计数 + 队列快照 |
| GET | `/api/tasks/random_sample` | 随机抽查一条完成内容 |
| GET | `/api/stream/events` | SSE 实时事件流 |
| GET | `/api/stream/state` | 进度快照（含 debug） |
| GET/POST | `/api/admin/*` | 后台：状态/检修/重启/导出/清空/日志/用户管理 |
| GET/POST/PUT/DELETE | `/api/prompts*` | 提示词库：`/catalog` 环节+系统默认；`?actor=` 列表（admin 看全部）；增改删带 actor 鉴权 |
| GET/POST/DELETE | `/api/styles*` | 风格关键词库（2026-08-31，见 4.9）：列表/增删/CSV 导入（`/import`）/模板下载（`/template`）/选用预览（`/lookup`）/使用统计（`/stats`）/个人默认（`/default` POST 钉选、DELETE 取消）；公共库仅 admin 可写。**风格开局（2026-09-02）**：`/templates` 内置样卡、`/onboarding_state` 是否需引导、`/clone_templates` 克隆到个人库+pin 默认、`/learn` 样例图 VL 提炼风格草稿（multipart，1-5 张 ≤15MB，不落库） |
| POST | `/api/tasks/{id}/page_copy` | 手动编辑分页文案（2026-09-01：`{actor, page_index, body}`，1-200 字、页码 1-6 校验，仅终态任务，严格属主；配合弹窗「✎ 改文案」+ 定点重生成，见 4.4） |
| GET | `/healthz` | 健康检查 |

---

## 9. 模型与外部依赖

### 9.1 文本模型（`gateway/failover.py`）
- 主 `deepseek/deepseek-v4-pro`，备 `anthropic/k3`（Kimi，走 `https://api.kimi.com/coding`）。
- **已知问题**：`deepseek/deepseek-v4-pro` 这个 model ID 大概率是错的（会失败/空响应），实际跑通靠 Kimi 兜底。**重开发时建议核对 DeepSeek 的正确 model ID**，或直接以 Kimi 为主。

### 9.2 生图（`gateway/image_gen.py`）
- **双 provider（2026-08-28，`config.image_provider`）**：`openai_images`（默认）= OpenAI Images 协议（gpt-image-2 等）；`gemini` = Gemini generateContent 协议（`gemini-3-pro-image`，原生多模态，参考图以 `inlineData` 内联进 contents，无独立 edits 端点；响应取 candidates 里含 inlineData 的 part，图片以 data URI 内存中转，`fetch_image_bytes` 已支持解码，下游随即本地化落盘）。Gemini 尺寸用 `imageConfig.aspectRatio`（1152x1536→3:4，`_aspect_of` 换算）+ `imageSize` 档（`gemini_image_size`，1K/2K/4K）；生成可能数分钟，超时 600s。**任务级选择（2026-08-28 用户要求，迁移 010 `tasks.image_provider`）：生图默认 gpt-image-2，其它模型仅在用户手动选择时生效**——「待生图」确认卡有模型下拉（`POST /api/tasks/{id}/image_model`，仅 draft/confirm_gen 可改，严格属主），NULL=全局默认；`generate_image(provider=...)`/`cost_per_image(provider=...)` 参数优先于全局配置，asset_gen/定点重生成（partial_regen 沿用任务所选）/成本记账全链路透传。
- **fusionaix 统一网关（2026-08-28 实测接入）**：`https://api.fusionaix.cn`，Bearer 鉴权，一个 key 通调 gpt-image-2（¥0.2/0.4/0.6 @1K/2K/4K）/ gemini-3-pro-image（¥0.5/0.6/0.9）/ grok-imagine-image-quality（¥0.5 起，OpenAI 协议）。gpt-image-2 的 generations/edits 与现有代码字段全兼容（实测 1152x1536 + quality=high + multipart `image[]` 双参考图均 200），切它只需改 `OPENAI_IMAGE_BASE_URL`/`OPENAI_IMAGE_API_KEY` 两个环境变量。响应头 `X-Request-Id` 可查调用记录。**注意 VPS 宿主机 DNS（systemd-resolved）2026-08-28 起故障，全域名解析失败——qvp-dev compose 已加 `dns: [223.5.5.5, 223.6.6.6]` 修复（备份 docker-compose.yml.bak-20260828），生产 ~/qvp 同样受影响待修**。
- `gpt-image-2`，尺寸 `1152x1536`（3:4），质量档 `image_quality`（默认 `high`，2026-08-27 官方建议：中文信息图文字渲染用 high）。文生图走 `/images/generations`，图生图走 `/images/edits`（multipart，参考图字段名 `image[]`）。
- **成本取价按 provider**：`image_gen.cost_per_image()` 统一返回当前 provider 单价（gemini 走 `gemini_image_cost_per_image_cny` 默认 0.6@2K，否则 `image_cost_per_image_cny` 默认 0.2），asset_gen/重生成两处记账都走它，不要直接读 settings。
- **quality 兼容降级**：`_post_with_quality_fallback` 统一发请求，网关不认识 quality 返回 400 时自动去掉该参数重试一次并留痕（防 openox 等转发网关升级后整体失败）。
- gpt-image-2 编辑**不要传 `input_fidelity`**（会 400）。
- 响应字段 `data[0].url`，已加 `response_format="url"` + 防御解析。
- **`mock_image_gen=true` 时返回占位 SVG**（不调 API）。

### 9.3 搜图/搜索
- 实景图：`image_search.py`（默认 OpenSERP `http://127.0.0.1:7001`）。
- 证据包：`web_search.py`（豆包搜索 API + DeepSeek 联网验证）。

---

## 10. 已知问题与坑

0. **2026-08-25 用户反馈整改（P0 已落地）**：反馈 5 条——文字扭曲概率大、实景图使用错误、对比类每页需双主体同框、对比需多角度、图上文字太多。已做两项：
   - **图上文字减量**（`prompt_versions.py`）：生图共享风格词文字上限 100→40 字；`PAGES_PROMPT` 封面 20-40→12-20 字、要点页 40-90→25-50 字且每页只保留一个核心要点；`PAGE_REGEN_PROMPT` 同步收紧到 25-50 字。
   - **出图文字质检门**（`nodes.py` 的 `_text_quality_gate` + `text_similarity`）：非 mock 模式下每张图产出后立即 OCR，与分页文案算字级相似度（`text_similarity`，忽略标点空白），低于 `OCR_TEXT_SIMILARITY_THRESHOLD`（默认 0.85）自动换构图重生成，最多 `ASSET_TEXT_MAX_ATTEMPTS`（默认 2）次；仍不达标在 `model_version` 打 `|text_garble:相似度` 标记交人工审核；OCR 服务异常不阻塞出图。OCR 费用计入 asset_gen 节点成本。测试 `tests/integration/test_asset_text_gate.py` + `tests/unit/test_text_similarity.py`。
   - **待做（P2）**：搜图结果用 qwen-vl 做主体相关性校验；若扭曲率仍高，终极方案是 AI 出背景 + 代码贴字（Pillow 排版，文字 100% 正确）。
   - **P1 已落地（2026-08-25，对应反馈②③④）**：compare 模式 `entity_bind` 先用 LLM 把 query 拆成主体 A/B（`_split_compare_subjects`，LLM 失败退回连词启发式 `_heuristic_split_subjects`，再失败退回整词搜索旧行为），各搜整体图 3 张 + 「细节 侧面」图 2 张，Asset.subject 打 `A:名称` / `B:名称（细节）` 标签；`asset_gen` 按页轮换喂 A、B 池各 2 张参考图（`_page_refs`，无标签存量图退回全量喂）；compare 生图提示词「尽量同时呈现两个主体」改为硬性「每页必须双主体同框对比 + 不同页不同角度」。测试 `tests/integration/test_compare_subjects.py` + `tests/unit/test_subject_split.py`。
1. **迁移没补跑到开发库会崩**：`tasks.mode` 列必须 `psql -f migrations/001_initial_schema.sql` 应用；否则 `SELECT tasks.mode` 报「WITHIN GROUP is required for ordered-set aggregate mode」（`mode` 撞 PG 的 `mode()` 聚合函数名）。
2. **DeepSeek 主模型 ID 疑似错误**：正文实际靠 Kimi 兜底。（2026-08-19 实测：`deepseek-v4-pro` ID 有效，但它是推理模型，litellm 低 max_tokens 下 content 为空 → 触发兜底。待后续处理。）
   - **已定位根因并修复（2026-08-31）**：`deepseek-v4-pro`/`k3` 均为推理模型，max_tokens 1024 的额度被 reasoning 占满 → 正文空 → 双链降级失败（draft_gen RuntimeError 的根因）。max_tokens 已统一提到 4096，空响应错误带 `finish_reason` 诊断。
   - 另已修复（2026-08-20）：幂等曾把**失败节点**当已完成跳过（node_events 失败也写 finished_at），导致重试不生图直接跑完。现在只跳过 `error_class IS NULL` 的节点。
3. **openox.net（$3 测试账户）生图不稳定**：SSL record layer failure / 502 / 400 upstream，且慢（实测每张 1-3 分钟，晚间更慢，偶发 ReadTimeout/ConnectError——失败任务删除该节点 error 事件后 retry 即可）。**开发阶段用 `MOCK_IMAGE_GEN=true`**。另注意其上游有两类 URL：manxiaobai 临时链接（隔天 404）和 4-5MB "签名内联 URL"（浏览器无法加载），`gateway/ocr.py` 的 `fetch_image_bytes` 已统一兼容（本地路径/内联解码/防盗链抓取），OCR/导出/展示代理都走它。
   - **教训（2026-08-20）**：`_dedupe_and_validate` 曾因缺 `import hashlib` + 静默 except 导致去重/尺寸校验/本地持久化全部静默失效。修改后务必确认 `static/generated/` 有文件落盘。
   - **教训 2（2026-08-20）**：图生图曾整体静默降级为文生图——`_download_image_bytes` 用 httpx 裸 GET 参考图，本地 `/static/` 路径抛 `UnsupportedProtocol`、防盗链图床 403，均被 `except httpx.HTTPError` 吞掉。已改为复用 `fetch_image_bytes`，降级时 `print` 留痕（日志搜「图生图失败，降级文生图」）。验证方式：真实跑一次 edits 调用，肉眼比对生成图与参考图。
4. ~~**OCR 是桩**~~（已解决 2026-08-20）：真实 OCR 已接入阿里百炼 `qwen-vl-ocr`（`gateway/ocr.py`，模型可用 `OCR_MODEL` 配置），cross_check 用真实识别文字与分页文案对撞，风险分级从此有参考意义。mock 生图时 OCR 仍走桩。
5. ~~**对比模式搜图**~~（已解决 2026-08-25）：compare 已拆主体 A/B 分搜并打标签，asset_gen 按页轮换喂双主体参考图（见本节第 0 条 P1）。
6. **并发：统计复用 AIMD（2026-08-21 改版）**：`INITIAL=2 / MIN=1 / MAX=4`。`PriorityQueue` 多路排序（urgent > normal > scheduled，同级 FIFO，重试保留原优先级）；limiter 加性增（连续成功 ≥ max(2,容量×2) 才 +1）/乘性减（限流即减半），**限流后 120s 冷却期内只积累成功不升容**（防抖振荡）；节点级 429 由 `execute_node` 即时上报（`report_throttle`），不等任务收尾。生图仍在任务内串行+间隔，多任务并发时上游压力 = 当前容量。**任务内 6 页并行出图（2026-09-01，`image_gen_parallel`，借鉴 8003）**：>1 时 6 页 Semaphore 限流并发（去重/尺寸校验/文字合成/OCR/VL 关卡仍在页内串行，`seen_hashes` 由 asyncio 单线程协程共享无竞态，结果按页码保序）；=1 保持串行+`image_gen_delay_seconds` 间隔旧行为。8003 生产用 6；我们的 fusionaix 通道限额未实测，先在 8002 设 2-3 试。
7. **事件总线是进程内的**：单 worker uvicorn 够用，多 worker 需换 Redis pub/sub。
8. **服务器部署三教训（2026-08-21）**：
   - **Pillow 必须在 requirements.txt**：pyproject.toml 里有但镜像按 requirements.txt 装，漏了会让 `_dedupe_and_validate` 的 `from PIL import Image` 抛 ModuleNotFoundError——有 except 兜底不阻塞出图，但去重/尺寸校验/本地持久化全部失效，只有日志 traceback 留痕。改依赖后服务器要 `docker compose build app`（容器内 pip install 在 recreate 后丢失）。
   - **init_db.py 只补不重置**：旧版每次重启把三账号密码 UPDATE 回初始值，后台改的密码重启即失效。现改为已存在账号跳过；同时按文件名顺序应用全部迁移（不再只跑 001），并排除 macOS AppleDouble 文件（`._*.sql`，pathlib glob 会匹配，read_text 直接 UnicodeDecodeError 让容器起不来）。conftest 同样处理。
   - **override 挂载**：vpstest04 的 `docker-compose.override.yml` 挂了 `static/src/init_db.py/migrations` 四个目录，改这些文件 scp 后 `docker compose restart app` 即可；改 requirements.txt 才需要 build。

---

## 11. 给 KimiCode 的重开发建议

1. **生图先 mock**：`MOCK_IMAGE_GEN=true`，别在开发阶段烧钱/等接口。
2. **三种模式是核心业务逻辑**：`tasks.mode` + 6 条提示词 + 生图方式（文生图 vs 图生图）必须保留。
3. **流水线 14 节点顺序 + 幂等**别乱动；改动集中在 `nodes.py` 各节点实现。
4. **前端已重做为 Vue3 SPA**（2026-08-19）：`static/index.html` + `views/`（Login/Dashboard/Import/Tasks/Review/Sample/Admin）+ `components/`（Layout/StepsBar）+ `api.js`（fetch 封装 + 全局状态/模式/风险口径）+ `vendor/`（本地 vue@3.5.18、vue-router@4.5.1，无构建链，hash 路由）。保留了 mode 导入入口、流式监控（并入任务中心）、debug 展示。
5. **字体**：`static/fonts/` 是思源宋体（Noto Serif SC），`common.css` 已 `@import` 并设为全局 `font-family`；重做前端记得继续引用。
6. **`.env` 别提交**（gitignored），交接时单独给真实 key。
7. **跑通判定**：导入一条 Query → 14 节点全绿 → 任务 `status=done`（当前 mock 生图下已验证可跑通）。

### 11.1 后续研究方向（2026-09-01 调研 8003 记录，效果不佳时的备选方案）

- **文案前置核查关卡（8003 text_check.py）**：生文后、生图前挂起 `awaiting_text`——LLM 一次产出错别字自查 + 正文草稿 + 6 页分页草稿，人工核查放行才生图；支持全新起草/手工正文导入改写/驳回定向修改（只改标记处）三种模式。文案在生图前定稿，事后弹窗修正/定点重生成的使用频率会大降。改造量：任务状态机加挂起态 + 3-4 个接口 + 前端核查页（四个候选里最大）。若「先生文确认再生图」的工作流仍挡不住文案问题，优先做这个。
- **英文视觉扩写（8003 visual_writer.py）**：把中文分页文案先扩写成英文视觉描述（主体/场景/光位/视角/色彩，每页 40-80 词），拼进全英文约束骨架（底座铁律英译版）喂 gpt-image-2，中文文案原样保留逐字渲染——依据是 gpt-image 对英文视觉指令理解更准，ChatGPT 网页端质量高就高在这层改写。**注意**：8003 只把它接在回退路径、主路径空转，效果未经他们主路径验证；我们若做，先小批量 A/B 再全量。改造点：`prompt_versions.py` 加英文骨架常量 + asset_gen 加一次 `call_with_failover` 扩写调用。
- **参考图人工确认关卡（8003 ref_collect.py）**：compare/single 搜图后挂起 `awaiting_refs`，候选落库 `selection_status=candidate`，人工勾选 confirmed 才进生图（含 OCR 命中关键词初筛排序）。若实拍参考图质量投诉持续，可做；改造量与文案关卡同级。
- **生图多通道轮询（8003 image_gen.py）**：`IMAGE_GEN_CHANNELS` 配多通道游标轮询 + 失败跨通道降级（先保图生图语义再降级文生图）。需要先拿到第二个 gpt-image 通道 key 才有意义。

---

## 12. 提交历史

本文档只记录到三模式落地的早期历史；**2026-08-27 接手后的完整演进看 `git log dev`**（中文 conventional commit，每个提交信息含改动清单与测试结果）。最近一轮（2026-09-01）：风格系统/实图治理/文字呈现/弹窗标记与改文案/标杆规范/冲突防护/人工样例学习，见 4.2/4.4/4.8/4.9 各节。

早期历史（feat/three-modes 分支）：

```
b73fdce feat: 生图可模拟 + 降并发串行 + 退避重试
1b9fe70 feat: 流水线 debug 流式输出 + 思源宋体全局字体
1925e33 feat: 生图模型切 gpt-image-2（3:4 竖版）
d3889fb fix: 生图响应 url 兜底 + 尺寸口径统一为竖版(2:3)
3cf41e3 feat: asset_gen 按 mode 分支（图生图 vs 文生图）
7c3b25d feat: 生图网关切 gpt-image-1.5（文生图 + 图生图）
bc27116 feat: entity_bind 在 general 模式跳过搜图
2168166 feat: draft_gen 按 mode 选正文提示词
10f7a43 feat: 导入流程加 mode 字段（API + CSV + 前端）
c9d0370 feat: 提示词按 mode 提供 6 条（正文+生图）
07c1a85 feat: tasks 加 mode 列（compare/single/general）
c27d13f docs: 三生产模式 design + implementation plan
fd0900e feat: 流式监控 + 后台管理 + 进度增强
74c20a9 feat: Docker 打包（应用 + PG + Redis + OpenSERP 一键编排）
```

> 设计 spec：`docs/superpowers/specs/2026-08-17-三生产模式-design.md`
> 实现计划：`docs/superpowers/plans/2026-08-17-三生产模式-implementation.md`
> 路线图：`docs/开发计划.md`；模型选型：`docs/模型选择与替换分析.md`
